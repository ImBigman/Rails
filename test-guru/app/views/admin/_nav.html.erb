<nav class="navbar navbar-expand-lg navbar-dark bg-admin">
  <%=link_to image_tag("/test_logo.png", height: '100', width: '150'), admin_tests_path, class:"navbar-brand"%>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav font-weight-bolder text-uppercase h5">
      <% if current_user.is_a?(Admin) %>
        <%= octicon "home", height: 24, class: "mt-2" %>  <%=link_to t('nav-home'), admin_tests_path, rel: "nofollow", class:"nav-item nav-link"%>
      <% else %>
        <%= octicon "home", height: 24, class: "mt-2" %>  <%=link_to t('nav-home'), root_path, rel: "nofollow", class:"nav-item nav-link"%>
      <% end %>
    </div>
  </div>
  <span class="navbar-text font-weight-bolder rounded-lg text-white h6">
    <% if user_signed_in? %>
      <%= t('hello', name: current_user.login) %> <%= octicon "sign-out", height: 24, class: 'pl-1' %>
      <%= link_to t('actions.logout'),  destroy_user_session_path, method: :delete, class: 'link text-decoration-none text-white' %>
      <br><%= octicon "verified", height: 24, class: 'pl-1' %> <%= link_to t('actions.badge-progress'), admin_badges_path, class: 'link text-decoration-none text-lowercase text-white' %>
    <% end %><br>
  </span>
</nav>




