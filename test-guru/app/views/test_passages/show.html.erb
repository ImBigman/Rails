<h2><%= t('.header',title: @test_passage.test.title) %></h2>
<%= content_tag :h5,t('.counter', current_question: @current_question_number, all_questions: @all_questions), id: 'progress', data: {test_progress: @progress_percent.to_s + '%' }%>

<div class="progress" style="height: 40px;">
  <div class="progress-bar bg-warning font-weight-bolder text-body" role="progressbar" style="width: 10%" aria-valuenow="10" aria-valuemin="0" aria-valuemax="100"><%= @progress_percent %>%</div>
</div>

<h3>
  <%= t('.title', title: @test_passage.current_question.body) %>
</h3>
<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids,  @test_passage.current_question.answers, :id, :body, include_hidden: false  do |a|%>
    <p style="padding: 10px">
      <%= a.check_box id: "answer_ids_#{a.object.id}"%>
      <%= a.label %>
    </p>
  <% end %><br>

  <p><%= link_to t('.gist link'), gist_test_passage_path(@test_passage), method: :post, class:'btn btn-outline-info' %>
  <p class="next-question"><%= form.submit t('.next') %></p>
<% end %>
